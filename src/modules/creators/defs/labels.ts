import { CrudLabels } from '@common/defs/types';

const Labels: CrudLabels = {
  CreateNewOne: 'list.create_new_creator',
  NewOne: 'list.new_creator',
  ReadAll: 'list.read_all_creators',
  Items: 'list.creators',
  EditOne: 'list.edit_creator',
};

export default Labels;
