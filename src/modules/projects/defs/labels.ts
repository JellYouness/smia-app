import { CrudLabels } from '@common/defs/types';

const Labels: CrudLabels = {
  CreateNewOne: 'project.create_new_one',
  NewOne: 'project.new_one',
  ReadAll: 'project.read_all',
  Items: 'project.items',
  EditOne: 'project.edit_one',
  ReadOne: 'project.read_one',
};

export default Labels;
