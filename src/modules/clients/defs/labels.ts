import { CrudLabels } from '@common/defs/types';

const Labels: CrudLabels = {
  CreateNewOne: 'list.create_new_client',
  NewOne: 'list.new_client',
  ReadAll: 'list.read_all_clients',
  Items: 'list.clients',
  EditOne: 'list.edit_client',
};

export default Labels;
