import { CrudLabels } from '@common/defs/types';

const Labels: CrudLabels = {
  CreateNewOne: 'media.create_new_one',
  NewOne: 'media.new_one',
  ReadAll: 'media.read_all',
  Items: 'media.items',
  EditOne: 'media.edit_one',
  ReadOne: 'media.read_one',
};

export default Labels;
